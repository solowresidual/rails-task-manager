<div class="container">
  <%= simple_form_for(task) do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <% if task.id %>
      <%= f.input :completed %>
    <% end %>
    <%= f.submit %>
  <% end %>
</div>
